sync:
  services:
    dev-eks-blue:
      blue:
        branch: main
        spec: dev-eks/eks-blue-sync.yaml
    dev-eks-green:
      green:
        branch: main
        spec: dev-eks/eks-green-sync.yaml
